import './globals.css'

export const metadata = {
  title: 'lain: neural network',
  description: 'Layer connections across consciousness',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
